"""
Capa de modelos (SQLModel) — el mapa entre Python y las tablas.

Acá definimos qué son las cosas (entidades) y cómo se guardan. En el
Módulo 02 esto no existía: el "modelo" era la tabla SQL escrita a mano
en `schema.sql`. Ahora una clase de Python define la tabla.
"""
