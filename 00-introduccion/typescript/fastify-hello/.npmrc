# Configuración de pnpm para el proyecto
# ============================================
# ignore-scripts: evita que pnpm ejecute scripts de build de dependencias
# como esbuild. Las dependencias ya funcionan sin ellos.
ignore-scripts=true

# package-manager-strict: desactiva la validación estricta del package manager
package-manager-strict=false
